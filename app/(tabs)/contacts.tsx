// Fichier: app/(tabs)/contacts.tsx
// Ce fichier est très court, car il ne fait qu'importer le composant réel.

import ContactsScreen from '../../components/ContactsScreen';

export default function TabContactsScreen() {
    return (
        // Le composant UI réel est appelé ici
        <ContactsScreen />
    );
}